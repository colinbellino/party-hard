<div class="name">
  {{drop.name}}
  <span class="quantity">×{{drop.quantity}}</span>
</div>

<div class="price">
  {{drop.price}}
</div>

{{#if drop.deptors}}
  {{drop-deptor-list deptors=drop.deptors}}
{{/if}}

<div class="price-per-character">
  {{drop.pricePerPerson}}
</div>
